export interface rowStatsProps {
  title: string;
  number: number;
  percentage: number;
  color: string;
}

export interface cardProps {
  id: string;
  title: string;
  url_image: string;
  publisher: string;
  author: string;
  themes: string[];
}
